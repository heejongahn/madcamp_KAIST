extends layout

block content
  h1 Sign in
  form(action="POST")
    .form-group
      label(for="accountid") 계정 이름
      input(type="text")#accountid.form-control
    .form-group
      label(for="password") 비밀번호
      input(type="password")#password.form-control
